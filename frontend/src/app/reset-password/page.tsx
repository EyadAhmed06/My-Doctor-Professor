import { Suspense } from "react";
import { ResetPasswordPage } from "@/components/account-action-page";
export default function Page(){return <Suspense fallback={null}><ResetPasswordPage/></Suspense>;}
