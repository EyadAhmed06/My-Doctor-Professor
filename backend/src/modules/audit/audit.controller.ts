import { Controller, Get, NotImplementedException, Param, Query, UseGuards } from '@nestjs/common'; import { Roles } from '../auth/decorators/roles.decorator'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { RolesGuard } from '../auth/guards/roles.guard';
@Controller('audit-logs') @UseGuards(JwtAuthGuard,RolesGuard) @Roles('SYSTEM_ADMIN')
export class AuditController {private pending():never{throw new NotImplementedException('Audit workflow will be implemented after DTO contracts');} @Get() list(@Query() _query:Record<string,string>){return this.pending();} @Get('export/file') export(@Query() _query:Record<string,string>){return this.pending();} @Get(':auditId') getOne(@Param('auditId') _id:string){return this.pending();}}
