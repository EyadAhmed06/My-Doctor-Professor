import { Body, Controller, Get, NotImplementedException, Param, Post, Put, UseGuards } from '@nestjs/common'; import { CurrentUser } from '../auth/decorators/current-user.decorator'; import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard';
@Controller('users') @UseGuards(JwtAuthGuard)
export class UsersController {private pending():never{throw new NotImplementedException('User workflow will be implemented after DTO contracts');} @Get(':userId') getOne(@Param('userId') _id:string,@CurrentUser() _current:unknown){return this.pending();} @Put(':userId') update(@Param('userId') _id:string,@Body() _body:unknown,@CurrentUser() _current:unknown){return this.pending();} @Post(':userId/change-password') changePassword(@Param('userId') _id:string,@Body() _body:unknown,@CurrentUser() _current:unknown){return this.pending();}}
